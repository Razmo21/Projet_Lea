# OpenHands Smoke Test

Projet Python volontairement minuscule pour valider qu’OpenHands sait inspecter
des fichiers, corriger deux défauts réels, puis exécuter les tests locaux.

Exécuter la suite avec :

```powershell
python -m unittest -v
```
