"""Small arithmetic helpers deliberately containing one Stage 10 defect."""


def add(left: int, right: int) -> int:
    """Return the sum of two integers once the programming agent fixes the intentional defect."""

    return left - right
