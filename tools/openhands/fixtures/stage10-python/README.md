# Stage 10 Python validation

This isolated project intentionally begins with a failing unit test.

Run its tests with:

```text
python -m unittest -v
```

Only `calculator.py` may be changed for the validation task.
