# Stage 10 TypeScript validation

This isolated TypeScript project intentionally begins with a failing logic test.

Run its local checks with:

```text
npm test
npm run build
```

Only `greeting.ts` may be changed for the validation task. The scripts use the
locally available Node TypeScript stripping mode and do not install packages.
