/** Format the greeting text shown by a small React-compatible view model. */
export function greeting(name: string): string {
  /** Return the intentionally incorrect wording repaired during the validation run. */
  return `Hello ${name}`
}
